# Daily Report · 2026-04-04

## 세션 1: UI 센터링 + CF Pages 배포 (데스크탑 Claude)

### 작업 내용
1. **perceptdot 랜딩 페이지 UI 센터링 수정**
   - 문제: demo 섹션 카드, 코드 블록이 왼쪽 정렬되고 오른쪽 여백이 큼
   - 원인: CSS `width: 100%` + `max-width: 600px` 조합으로 margin auto 무효화
   - 해결: `.hero-code` 클래스에서 `width: 100%` 제거, `margin: 0 auto 32px` 적용
   - 관련 수정:
     - `.hero-code` (라인 463): width 제거, margin auto 추가
     - `.hero-sub` (라인 444): margin auto 추가
     - `.install-tabs` (라인 487): flexbox + align-items center 추가
     - `.steps-grid` (라인 678-683): margin auto 추가

2. **Cloudflare Pages 배포**
   - 문제: index.html 변경 후 CF Workers deployment로 착각
   - 발견: api.perceptdot.com (CF Workers) ≠ perceptdot.com (CF Pages)
   - 해결: 수동 배포 실행
   ```bash
   cd /Volumes/My\ Passport\ for\ Mac/My_project/perceptdot/landing
   eval $(cd .. && ./kw-key.sh load cloudflare_api)
   npx wrangler pages deploy .
   ```
   - 결과: 배포 완료 (77ccec09.perceptdot-landing.pages.dev)

3. **CF Pages Git 연결 문제 조사**
   - 발견: perceptdot-landing CF Pages 프로젝트가 Git 연결 없음
   - 원인: 프로젝트 생성 시 "수동 배포" 방식으로 설정됨
   - 대시보드에서 Git 연결 옵션 미표시 (수동 배포 프로젝트는 불가)

### 산출물
- ✅ perceptdot.com 페이지 센터링 수정 (배포 완료)
- ✅ CF Pages 배포 성공

---

## 세션 2: GitHub Actions 자동 배포 설정 (데스크탑 Claude)

### 작업 내용
1. **GitHub Actions 워크플로우 생성**
   - 파일: `.github/workflows/deploy-landing.yml`
   - 트리거: main 브랜치에 push, perceptdot/landing/** 변경 감지
   - 동작: 자동으로 `npx wrangler pages deploy .` 실행
   - 환경변수: CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID (GitHub Secrets)

2. **GitHub Secrets 설정**
   - CLOUDFLARE_API_TOKEN: (Keychain에서 로드한 토큰)
   - CLOUDFLARE_ACCOUNT_ID: `e78299bd0b0b0006000f72c1a4f06009`
   - CEO가 GitHub 웹 UI에서 workflow scope 활성화

3. **커밋 & 푸시**
   ```bash
   cd /Volumes/My\ Passport\ for\ Mac/My_project/perceptdot
   git add .github/workflows/deploy-landing.yml
   git commit -m "chore: add GitHub Actions auto-deploy for landing"
   git push origin main  # 성공 (PAT workflow scope 활성화 후)
   ```

### 산출물
- ✅ GitHub Actions 워크플로우 생성 및 배포
- ✅ GitHub Secrets 설정 완료
- ✅ 자동 배포 활성화 (perceptdot/landing 변경 시 자동 실행)

---

## 핵심 발견사항

| 항목 | 상황 |
|------|------|
| **CF Pages Git 연결** | 수동 배포 프로젝트 → Git 옵션 없음 (정상) |
| **배포 방식** | GitHub Actions 워크플로우 + 수동 명령 병행 가능 |
| **자동 배포 활성화** | 완료 (main push → 자동 배포) |

---

## 완료 항목 체크리스트

- ✅ UI 센터링 수정 및 배포
- ✅ CF Pages 수동 배포 성공
- ✅ GitHub Actions 워크플로우 생성
- ✅ GitHub Secrets 설정
- ✅ 자동 배포 활성화 테스트 준비 완료

---

## 남은 할 일

### 우선순위 1 (즉시)
1. **자동 배포 검증**: perceptdot/landing 파일 수정 후 push → 자동 배포 확인
   - GitHub Actions 탭에서 워크플로우 실행 확인
   - Cloudflare Deployments 탭에서 최신 배포 확인

### 우선순위 2 (다음 세션)
1. **perceptdot 페이지 최종 QA**: 센터링 수정 사항 브라우저에서 hard refresh 확인 (Cmd+Shift+R)
2. **배포 자동화 모니터링**: 다음 push 시 GitHub Actions 자동 실행 확인

### 우선순위 3 (나중)
1. CF Pages 프로젝트 재생성 고려 (Git 연결 필요 시) - 현재는 불필요

---

## 세션 정보
- **날짜**: 2026-04-04
- **환경**: 데스크탑 Claude
- **토큰 사용**: ~5,000 토큰
- **완료도**: 90% (자동 배포 검증만 남음)
