# Daily Report — 2026-03-26 (데스크탑 Claude)

## 세션 요약
- **작업**: GitHub MCP 연결 테스트

## 작업 내용

### MCP GitHub 테스트
- `github_prs` 도구 호출 → **401 Unauthorized** 오류
- URL: `https://api.github.com/repos/perceptdot/percept/pulls`

### 토큰 직접 유효성 확인
- `GITHUB_TOKEN` curl 테스트 → **200 ✅**
- `VERCEL_TOKEN` curl 테스트 → **200 ✅**

### 원인 분석
- 토큰 자체는 유효
- 문제: MCP 프로세스(perceptdot github/vercel 서버)가 `settings.json` env 값을 전달받지 못함
- 좀비 MCP 프로세스 재발 가능성

### 해결 방향
- Claude Code 완전 재시작 후 재테스트 필요
- 필요 시 `pkill -f "perceptdot/sdk/packages"` → 새 세션

## 추가 세션 (4차)

### MCP 프로세스 강제 kill 테스트
- `pkill -f "perceptdot/sdk/packages"` → 4개 프로세스 완전 제거 확인
- 즉시 MCP 도구 재호출 → **서버 전체 disconnected** (도구 사용 불가)

### 구조 확인
- MCP 프로세스는 Claude Code가 직접 spawn/관리
- kill → 끊김, 재spawn → Claude Code 재시작 시에만 가능
- **근본 원인**: 프로세스 살아있어도 401 발생 = wrapper(`mcp-token-loader.sh`)가 토큰 전달 실패

## 미완료
- Claude Code 재시작 후 MCP GitHub/Vercel 재확인 필요
- wrapper 토큰 전달 경로 점검 필요

---

## 세션 7 (데스크탑 Claude) — visual_check 401 해결 작업

### 원인 분석
- `claude mcp list` → perceptdot: `https://mcp.perceptdot.com/mcp` — `?api_key=` 없음
- MCP Worker(`mcp.perceptdot.com`)가 `api_key` 쿼리 파라미터로 키를 받아 백엔드에 전달하는 구조
- URL에 키가 없으니 `apiKey=null` → 백엔드 401

### github/vercel과의 차이
- github/vercel: 로컬 Node.js process.env 캐싱 문제 (readEnvKey로 해결)
- visual_check: 원격 CF Worker URL에 api_key 파라미터 누락 — 완전히 다른 문제

### 최종 해결 (rotate_percept_key.py)
- settings.json의 기존 PERCEPT_API_KEY → KV team 플랜 등록
- 노출 키(pd_live_5309...) KV 삭제
- ~/.claude.json + ~/.claude/.mcp.json URL에 `?api_key=KEY` 추가
- Claude Code 재시작 완료

### 생성된 스크립트
- `scripts/rotate_percept_key.py` — 키 교체 원스텝 (KV등록+노출키삭제+URL업데이트)
- `scripts/store_key_direct.py` — Cloudflare REST API로 KV에 키 저장 (wrangler 우회용)

### visual_check vs github/vercel 차이
- github/vercel: 로컬 process.env 캐싱 → readEnvKey() 해결
- visual_check: 원격 CF Worker URL에 api_key 파라미터 누락 → URL 업데이트로 해결

### 남은 작업
- Claude Code 재시작 후 `visual_check https://kfortunewave.com` 최종 확인
- INFRA-MCP-01: 세션 종료 시 MCP 자동 kill hook
