# perceptdot Daily — 2026-07-03

## 세션 1 (데스크탑 Claude) — @perceptdot/eye npm 패키지 구축 + 생태계 상태 점검

### 현재 상태 스냅샷
- npm 게시: core 0.1.0 / ga4 0.2.6 / vercel 0.1.6 / sentry 0.1.5 / github 0.1.5 — 로컬·npm·공식 레지스트리 3자 버전 완전 동기화 ✅
- 공식 MCP 레지스트리(registry.modelcontextprotocol.io): 위 5종 모두 active/latest ✅ (2026-06-01 게시)
- 인프라 생존: mcp.perceptdot.com/mcp 200 / api.perceptdot.com 200 / perceptdot.com 200 ✅
- **주력 @perceptdot/eye — npm·레지스트리 양쪽 모두 부재였음** ← 이번 세션에서 패키지 구축 완료

### @perceptdot/eye 누락 원인 (CEO 질의 답변)
- eye는 Remote MCP(CF Workers, `claude mcp add --transport http`) 방식으로 출시되어 npm 게시 단계가 스킵됨
- 로컬 `mcp/package.json`이 `perceptdot-mcp` + `private: true` + bin 없음 → 게시 불가 상태로 방치
- 레지스트리는 remote(streamable-http) 등록도 지원하는데 그것도 미등록 — 6/1 레지스트리 게시 때 구 5종만 올림

### 작업 내역 (신규 패키지: sdk/packages/eye/)
- `package.json` — @perceptdot/eye v1.1.0 (라이브 원격 서버 serverInfo 버전과 일치), bin: eye
- `src/index.ts` — 공식 MCP SDK stdio 서버. visual_check(url, prompt, viewport, no_cache 풀스키마) + percept_roi_summary(절대 규칙 3 토큰 측정, 1콜=1,200토큰 절감 추정) + 429 지수 백오프 4회(원격과 동일)
- `README.md` — 에이전트 독자용 (원격/로컬/Cursor 설치 3종 + 에러 코드 대응표)
- `server.json` — 레지스트리 등록 매니페스트 (npm 패키지 + remote 엔드포인트 동시 등록)
- `sdk/package.json` — workspaces에 eye 추가 + 누락돼 있던 github/sentry도 추가

### 검증 결과
- 전체 6개 패키지 tsc 빌드 무결 ✅
- stdio 스모크: initialize/tools/list/percept_roi_summary ✅
- 에러 릴레이: 무효 키 → 401 메시지 그대로 전달 ✅
- 라이브 E2E 1회(실키, ~$0.002): visual_check 성공 ✅ — 초기 테스트 방식 문제로 크레딧 최대 2건 소모 가능
- ⚠️ 부수 발견: E2E에서 자사 랜딩 perceptdot.com에 시각 버그 검출 — [HIGH] 콘텐츠 섹션 overflow, [MEDIUM] 버튼 clipped

### 대기 (CEO 승인 필요 — 프로덕션 배포)
1. `npm publish` (sdk/packages/eye, npm 로그인 필요)
2. 레지스트리 등록: `mcp-publisher login github` → `mcp-publisher publish` (server.json 준비 완료)

### 개선 백로그 제안 (이번 점검에서 발견)
- P1: perceptdot.com 랜딩 시각 버그 2건 수정 (자사 제품이 검출 — 도그푸딩 사례로 마케팅 소재 가능)
- P1: 원격 서버 protocolVersion '2024-11-05' → 최신 스펙 갱신 (mcp/src/index.ts, 재배포 필요)
- P2: 원격 eye 서버에 토큰 측정 코드 부재 (절대 규칙 3 미준수) — percept_roi_summary 추가 검토
- P3: 기존 5종 패키지에도 server.json 추가 (레지스트리 갱신 자동화)

(데스크탑 Claude)

---

## 세션 2 (13:00~14:00 KST · 데스크탑 Claude) — CEO 승인 후 실행: 개선 3종 + 게시 준비 완료

> 플로우: 설계=Fable 메인 / 실행=sonnet 서브에이전트 3개 병렬 / 검증=Fable 게이트

### 실행 완료 (sonnet 3개 병렬 + Fable 검증)
1. **랜딩 시각 버그 2건 수정** (`landing/index.html`, 커밋 92f2454)
   - [HIGH] `.hero::before` 600px 글로우가 375px 뷰포트 오버플로우 → `.hero`에 `overflow:hidden`
   - [MEDIUM] 법적 모달 3종(Terms/Privacy/Refund) 닫기버튼이 스크롤 시 클리핑 → flex 헤더 구조로 스크롤 책임 본문 이전
   - QA 게이트(Playwright): 375/768/1280 오버플로우 0, 콘솔 에러 0, 모달 4,749px 스크롤 후에도 × 버튼 고정 확인, 스크린샷 육안 검수 통과
2. **원격 MCP 워커 갱신** (`mcp/src/index.ts`, `mcp/src/stdio.ts`)
   - protocolVersion 협상(2025-06-18/2025-03-26/2024-11-05), serverInfo v1.2.0, visual_check 응답에 토큰 측정 라인(절대 규칙 3), stdio 스키마 4파라미터 싱크
   - 검증: tsc(docker) 통과 + 로컬 Node 런타임 협상 3케이스 실증 (echo/echo/fallback)
   - ⚠️ **배포 대기** — wrangler 인증 필요
3. **server.json 6종** (eye + core/ga4/vercel/sentry/github) — 전부 라이브 레지스트리 `mcp-publisher validate` 통과 (description 100자 제한 4건은 Fable이 수정)

### 게시 준비 상태
- npm: `@perceptdot/eye@1.1.0` dry-run 통과 (6파일 6.5kB, mcpName 포함)
- 레지스트리: mcp-publisher v1.7.9 설치 완료(스크래치패드)
- git: main 푸시 완료 (92f2454)

### 블로커 (CEO 로그인 3종 — 로그인만 해주시면 실행은 Claude가)
1. `! npm login` → 후속: eye npm publish
2. `! npx wrangler login` → 후속: 워커 프로덕션 배포 + 랜딩 Pages 배포
3. mcp-publisher GitHub 디바이스 로그인 → 후속: 레지스트리 eye 등록 + 5종 갱신

### 배포 후 검증 계획 (예약)
- mcp.perceptdot.com 헬스 v1.2.0 + initialize 협상 확인
- 랜딩 배포 후 eye visual_check 재실행 → ✅ 전환 확인 (도그푸딩 스토리 완성)
