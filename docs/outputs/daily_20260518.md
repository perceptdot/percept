# Daily 2026-05-18 — perceptdot (보안 패치)

> 작업자: CPO | 세션: 1

---

## 세션 1 — GitHub PAT 보안 사고 대응

### 배경
`git remote -v` 실행 중 `.git/config`에 GitHub Personal Access Token이 평문으로 박혀 있음 발견.
- 노출 토큰: `260328_for_perceptdot` (Classic PAT, repo scope, no expiration)
- X API 키 재발급 이력(2026-03-22, GitGuardian 탐지)과 동일 유형 재발

### 조치 완료

| 단계 | 내용 | 결과 |
|---|---|---|
| 1 | 노출 토큰 `260328_for_perceptdot` GitHub에서 revoke | ✅ CEO 직접 삭제 |
| 2 | Fine-grained PAT 신규 발급 | ✅ `perceptdot-percept-rw-260518` (366일, perceptdot Org, percept repo 한정, Contents R/W) |
| 3 | 새 PAT → macOS Keychain 저장 | ✅ `perceptdot_github/GITHUB_TOKEN` (kw-key.sh) |
| 4 | git remote URL 평문 토큰 제거 | ✅ `https://github.com/perceptdot/percept.git` |
| 5 | git credential.helper = osxkeychain 전역 설정 | ✅ |
| 6 | 미사용 Classic PAT 2개 삭제 (`perceptdot_mcp`, `260325 claude-mcp`) | ✅ CEO 직접 삭제 |

### 보안 개선 결과
- 이전: Classic PAT + no expiration + git config 평문 저장 (3중 위험)
- 이후: Fine-grained + 366일 만료 + percept repo 한정 + osxkeychain 관리

### 잔여 작업
- CEO: perceptdot 디렉토리에서 첫 `git push` 시 새 토큰 1회 입력 → osxkeychain 자동 등록

### 별도 발견 (별도 세션)
- `deploy-landing.yml` — CLOUDFLARE_API_TOKEN GitHub Secret 만료로 CF Pages 자동 배포 비활성화 상태
- 복구 필요 시: CF 새 토큰 발급 → GitHub repo Secrets 갱신 → workflow push 트리거 재활성화

---

## 세션 2 — CPO 전면 점검 (정상 동작 · 코드 개선 · Codex 검증 · 성과 보고)

### Step 1: 정상 동작 확인
| 서비스 | 엔드포인트 | 결과 |
|--------|-----------|------|
| API | `api.perceptdot.com/health` | ✅ OK v0.3.0 |
| MCP | `mcp.perceptdot.com/mcp` | ✅ JSON-RPC 응답 정상 |
| Landing | `perceptdot.com` | ✅ LIVE (CF Pages) |

### Step 2: 코드 개선 — 수정 목록 (7건)

| ID | 분류 | 파일 | 내용 |
|----|------|------|------|
| P0-SEC-01 | 🔴 보안 | `api/src/index.ts` | `/v1/apikey/:email` — 응답에서 `key` 필드 제거 (무인증 키 노출) |
| P0-SEC-02 | 🔴 보안 | `api/src/index.ts` | `/v1/free-key` — IP 레이트 리밋 5/hour 추가 (cf-connecting-ip KV) |
| P1-BUG-01 | 🟡 버그 | `api/src/index.ts` | screenshot clip `width: 1280` 하드코딩 → `VIEWPORT_WIDTH` (모바일 375px 뷰포트 오캡처 수정) |
| P1-BUG-02 | 🟡 버그 | `api/src/index.ts` | `parseVerdictText` 내 미사용 `_foundIssues` 변수 제거 |
| P1-BUG-03 | 🟡 버그 | `api/src/index.ts` | `/v1/recommend/log` 중복 logEntry 구성 → 단일 경로로 리팩터 |
| P2-STALE | 🟢 품질 | `api/src/data/registry.ts` | `REGISTRY_UPDATED_AT` 2026-03-22 → 2026-05-18 갱신 |
| P2-DEAD | 🟢 품질 | `mcp/src/stdio.ts` | stdio `tools/call` 핸들러 추가 (API 프록시 + r.ok 체크) |
| MINOR | 🟢 품질 | `eye-action/src/index.js` | `.filter((l) => l !== null)` → `.filter(Boolean)` |

### Step 3: Codex 검증 결과
```
GATE: PASS — P1: 0건 / P2: 2건 (즉시 수정 완료)
P2-A: mcp/dist/stdio.js dist 빌드 미포함 → -f 강제 추가 커밋
P2-B: stdio API 오류 r.ok 미체크 → isError:true 정확 반환
```

배포 완료: `api.perceptdot.com` Version ID `f4430ebb` (2026-05-18 ~15:00 KST)

커밋 이력:
- `84f6a71` — 보안 패치 7건 (P0/P1/P2)
- `3286b3e` — stdio P2 추가 수정 + dist 빌드

---

### Step 4: 성과 보고 (KV 실측 데이터 기준)

#### 가입 현황
| 구분 | 수 | 비고 |
|------|----|------|
| 전체 API키 레코드 | **15개** | apikey: 프리픽스 기준 |
| stats:total_free 카운터 | 10 | 카운터 이전 5개는 통계 미집계 |
| 외부 실제 사용자 | **~4명** | rndsw@apaulsoftware.com, 2904276237@qq.com, tommyle8103@gmail.com + 1 |
| 내부/테스트 계정 | ~11개 | ceo-test, internal, test-*, logo.test 등 |

#### 플랜별 분포
| 플랜 | 수 | 매출 |
|------|----|------|
| Free | 15 | $0 |
| Pro | **0** | $0 |
| Team | **0** | $0 |
| **총 매출** | — | **$0** |

#### 활성 사용 현황
- sessions_tracked (인메모리): **0** (Worker 재시작 후 초기화됨 — MVP 한계)
- feedbacks:list: **0건** (아직 피드백 제출 없음)
- 일일 신규 가입: 최근 1건/주 수준 (05-11: 1건)

#### 핵심 지표 요약
```
가입자: 15명 (실사용자 ~4명)
유료 전환: 0명
매출: $0
피드백: 0건
API 호환성: ✅ 정상
```

---

## POST_TASK 완료 (세션 1 + 2)
- ✅ daily_20260518.md 업데이트 (이 파일)
- ✅ 보안 패치 7건 + Codex P2 2건 수정 배포 완료
- backlog.md / MEMORY.md 갱신 불필요 (전술 작업, 전략 결정 없음)

## [CPO] 작업 완료 — perceptdot 전면 점검 (데스크탑 Claude, 15:30 KST)
1. API/MCP 정상 동작 확인 (v0.3.0 LIVE)
2. 보안 P0 2건 + 버그 P1 3건 + 품질 P2 2건 수정 → 배포 완료 (Version f4430ebb)
3. Codex PASS (P1=0, P2=2 즉시 해결) / 실사용자 ~4명 / 매출 $0 / 유료 전환 0명
